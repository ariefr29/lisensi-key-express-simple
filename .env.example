# ===================================
# Server Configuration
# ===================================

# Port for the server to listen on
# Default: 3000
# Production: Set via hosting platform or leave as 3000
PORT=3000

# ===================================
# Security Settings
# ===================================

# JWT Secret Key for admin authentication
# IMPORTANT: Change this to a strong, unique value in production!
# Minimum: 32 characters
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-minimum-32-chars"

# Webhook Secret for auto-license generation
# IMPORTANT: Change this to a strong, unique value!
# This prevents unauthorized webhook requests
# Share this secret with your e-commerce platform webhook configuration
WEBHOOK_SECRET="your-webhook-secret-change-this-in-production"

# ===================================
# Admin Account (Initial Setup)
# ===================================

# Admin username for initial account creation
# Only used by `npm run create-admin` script
# You can change the password later via admin dashboard
ADMIN_USERNAME="admin"

# Admin password for initial account creation
# IMPORTANT: Change this immediately after first login!
# Minimum: 8 characters recommended
ADMIN_PASSWORD="admin123"
